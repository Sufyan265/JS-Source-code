<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Full page Chap_6 Practice</title>
    <style>
        html {
            height: 100%;
            width: 100%;
        }
    </style>
</head>
<body>
    <h2>Check console</h2>

    <script>

let again = true || false
const drive = (age) => {
  return age >= 18? true :false
}

while (again) {
  let age = prompt("What's your age ")
  age = Number.parseInt(age)
  if (age < 0) {
    console.error("Please! enter valid age")
    break;
  } 
  if (drive(age)) {
    alert("Yes you can drive")
  }
  else {
    alert("No you cannot drive")
  }
  again = confirm("Can you see prompt again? ")
}
/*
 // Q4
  let num = prompt("What's your number ")
  num = Number.parseInt(num)

  if (num > 4) {
    location.href = "http://google.com"
  }
*/
/*
// Q5 
let colr = prompt("Write color name ")
  document.body.style.background = colr
*/
    </script>
</body>
</html>